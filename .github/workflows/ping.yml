name: Ping Replit App

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl site
        run: |
          curl -I -L --max-time 20 https://code-interpreter-hub.replit.app/ || true
